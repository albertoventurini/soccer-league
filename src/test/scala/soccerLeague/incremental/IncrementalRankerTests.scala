package soccerLeague.incremental

import org.junit.runner.RunWith
import org.scalatest.FunSuite
import org.scalatest.junit.JUnitRunner
import soccerLeague.common.{LeagueRules, MatchResult}

@RunWith(classOf[JUnitRunner])
class IncrementalRankerTests extends FunSuite {

//  test("newIncrementalRanker") {
//    val rank = new IncrementalRank()
//      .addMatch(new MatchResult("team1", 1, "team2", 0))
//      .addMatch(new MatchResult("a", 2, "b", 4))
//  }

}
